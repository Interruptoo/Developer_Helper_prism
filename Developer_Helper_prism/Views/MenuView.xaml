<UserControl x:Class="Developer_Helper_prism.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Developer_Helper_prism.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <DockPanel LastChildFill="False" Style="{StaticResource ThemeDockPanel}">
        <ItemsControl DockPanel.Dock="Top" ItemsSource="{Binding MenuCollection}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="50"
                            Height="50"
                            Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding MenuName}"
                            ToolTip="{Binding MenuTooltip}">
                        <StackPanel>
                            <Image Width="20"
                                   Height="20"
                                   Source="{Binding MenuIcon}" />
                            <TextBlock Text="{Binding MenuTitle}" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ComboBox DockPanel.Dock="Bottom">
            <ComboBoxItem Content="XSUP" />
        </ComboBox>
    </DockPanel>
</UserControl>
